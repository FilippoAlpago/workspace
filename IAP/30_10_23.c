/*

*/
#include <stdio.h>
#include<stdlib.h>


int* noTwice(int* a,size_t a_size)
{
    int *out=(int*) malloc(a_size*sizeof(int));
    if(!out) exit(EXIT_FAILURE);
    int out_size=0;
    for(int i=0;i<a_size;i++)
    {


        
        for(size_t j=0;j<a_size;j++)
        {
            
        }
    }
}

int main()
{


    return 0;
}