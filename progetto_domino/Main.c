#include<stdlib.h>
#include<stdio.h>
#include "function_domino.h"


int main(int argc, char const *argv[])
{
    
    /*ListOfCard L=createListOfCard();
    addCardToList(L,createCard(1,2));
    addCardToList(L,createCard(5,6));
    addCardToList(L,createCard(2,2));
    freeCard(L,L->First->DX);
    freeCard(L,L->Last);
    freeCard(L,L->Last);
    
    free(L);*/
    
    //free(L);
    //free(L->First);
    //free(L->Last);
    //free(L);
    
    return 0;
}
